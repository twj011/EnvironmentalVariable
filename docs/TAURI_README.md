# 环境变量管理器 (Tauri + React)

一个现代化的 Windows 环境变量管理工具，使用 Rust Tauri 后端和 React 前端构建。

## 技术栈

### 后端
- **Rust** - 高性能系统编程语言
- **Tauri** - 轻量级桌面应用框架
- **winreg** - Windows 注册表操作
- **windows-rs** - Windows API 绑定

### 前端
- **React 19** - 现代化 UI 框架
- **TypeScript** - 类型安全
- **Vite** - 快速构建工具

## 功能特性

- ✅ 可视化管理用户和系统环境变量
- ✅ 智能 PATH 编辑器
- ✅ 实时生效（无需重启系统）
- ✅ 现代化暗色主题界面
- 🚧 智能路径优化（开发中）
- 🚧 备份与恢复（开发中）

## 开发环境要求

- Node.js 18+
- Rust 1.70+
- Windows 10/11

## 安装依赖

```bash
npm install
```

## 开发

启动开发服务器（热重载）：

```bash
npm run tauri:dev
```

## 构建

构建生产版本：

```bash
npm run tauri:build
```

构建产物位于 `src-tauri/target/release/`

## 项目结构

```
EnvironmentalVariable/
├── src/                      # React 前端源码
│   ├── App.tsx              # 主应用组件
│   ├── main.tsx             # 入口文件
│   └── styles.css           # 样式文件
├── src-tauri/               # Rust 后端源码
│   ├── src/
│   │   └── main.rs          # Tauri 主程序
│   ├── Cargo.toml           # Rust 依赖配置
│   └── tauri.conf.json      # Tauri 配置
├── index.html               # HTML 模板
├── vite.config.ts           # Vite 配置
├── tsconfig.json            # TypeScript 配置
└── package.json             # Node.js 依赖
```

## 迁移说明

本项目已从纯 Python (Flet) 方案迁移到 Rust Tauri + React 方案：

### 优势
- ⚡ 更快的启动速度和运行性能
- 📦 更小的安装包体积
- 🎨 更现代化的 UI 界面
- 🔒 更好的安全性和稳定性
- 🛠️ 更容易维护和扩展

### 原 Python 代码
原 Python 实现保留在以下目录：
- `core/` - 核心逻辑
- `gui/` - Flet GUI
- `main.py` - Python 入口

## 许可证

MIT License
